# TODO: Setup persistent storage (SQLite or JSON file).
# TODO: Define "get_user_info(user_id)" to fetch preferences/history.
# TODO: Define "update_user_info(user_id, key, value)" to save details.
# TODO: Ensure the LLM can trigger these via JSON schema instructions.